import HeroCalendar from './HeroCalendar';
import HeroTitle from './HeroTitle';

const Hero = () => {
    return (
        <div style={{font: '36px'}}>
            <HeroTitle />
            <HeroCalendar />
        </div>

    )
}

export default Hero;